### 解题思路

执行用时：0 ms, 在所有 Java 提交中击败了100.00%的用户
内存消耗：36.7 MB, 在所有 Java 提交中击败了93.37%的用户

### 代码

* java

```java
class Solution {
    public int removeElement(int[] nums, int val) {
        int pre = 0;
        for (int i = 0; i < nums.length; i++) {
            if (nums[i] != val)
                nums[pre++] = nums[i];
        }
        return pre;
    }
}
```

